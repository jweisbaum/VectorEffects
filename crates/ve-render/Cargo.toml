[package]
name = "ve-render"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Field evaluation, rasterisation, tiling, and the render cache."

[dependencies]
ve-core = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
rayon = "1.12.0"
blake3 = { version = "1.8.7", default-features = false, features = ["std"] }
wgpu = { version = "30.0.1", default-features = false, features = ["metal", "vulkan", "dx12", "wgsl"] }
pollster = "1.0.1"

[lints]
workspace = true
